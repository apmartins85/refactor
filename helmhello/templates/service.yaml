apiVersion: v1
kind: Service
metadata:
  name: {{ include "helmhello.fullname" . }}
  labels:
    {{- include "helmhello.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helmhello.selectorLabels" . | nindent 4 }}
