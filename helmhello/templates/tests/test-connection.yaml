apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helmhello.fullname" . }}-test-connection"
  labels:
{{ include "helmhello.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "helmhello.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
